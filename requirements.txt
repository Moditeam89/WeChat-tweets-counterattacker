# Python 依赖
# 微信 API
wechatpy>=2024.0.0          # 微信 SDK，用于公众号 API 调用
requests>=2.28.0           # HTTP 请求库

# 环境配置
python-dotenv>=1.0.0       # 从 .env 文件加载环境变量

# 数据处理
numpy>=1.24.0              # 数值计算，用于向量相似度计算
markdown>=3.4.0            # Markdown 解析

# 向量嵌入模型（Qwen3）
sentence-transformers>=2.2.0  # 句子嵌入，用于模板匹配
modelscope>=1.9.0             # ModelScope 工具链

# 标准库无需安装：json, os, sys, re, subprocess, concurrent.futures, io
